# 护理教材 AI 系统配置

top_k: 5

api_services:
  llm:
    model: "deepseek-v4-flash"
    base_url: "https://api.deepseek.com"
    api_key: ""              # 优先从 DEEPSEEK_API_KEY 环境变量读取
    temperature: 0.0
    max_tokens: 4096
