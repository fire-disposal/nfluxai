[project]
name = "nflux-ai"
version = "0.2.0"
description = "护理教材 AI 问答系统"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "jieba>=0.42",
    "openai>=1.0",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    "streamlit>=1.40.0",
]

[project.scripts]
nflux = "main:main"
